{
  "name": "agent",
  "version": "1.0.0",
  "main": "agent.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pm2": "pm2 start agent.js --name='TimerJob'",
    "dev": "npx nodemon -r dotenv/config ./agent"

  },
  "author": "",
  "license": "ISC",
  "keywords": [],
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.5",
    "moment": "^2.30.1",
    "mongodb": "^6.8.0",
    "mongoose": "^8.6.0"
  }
}
